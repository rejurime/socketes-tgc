using Microsoft.DirectX;

namespace AlumnoEjemplos.Socketes.Utils
{
    public class VectorUtils
    {
        public static string PrintVectorSinSaltos(Vector3 vector)
        {
            return "(" + vector.X + ", " + vector.Y + ", " + vector.Z + ")";
        }
    }
}